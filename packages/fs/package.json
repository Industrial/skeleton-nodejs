{
  "name": "@code9/fs",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.js",
  "typings": "./src/index.d.ts",
  "scripts": {
    "lint": "pnpm lint:tsc && pnpm lint:eslint",
    "lint:tsc": "tsc -p . --noEmit",
    "lint:eslint": "eslint --fix --cache --cache-location .eslintcache .",
    "test": "vitest"
  },
  "dependencies": {
    "@swc/helpers": "~0.5.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "@vitest/coverage-v8": "^1.2.2",
    "@vitest/spy": "^1.2.2",
    "@vitest/ui": "^1.0.4",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}